services:
  server:
    build:
      context: ./server
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env
    restart: unless-stopped

  client:
    build:
      context: ./client
    ports:
      - "4200:80"
    depends_on:
      - server
    restart: unless-stopped
